Search the source code of project dependencies for real implementation details.

Use this tool when you need to verify how a dependency actually works — API signatures, internal behavior, edge cases, type definitions — instead of relying on training data or documentation that may be outdated or incomplete.

The search is scoped to the exact version your project uses, so results are always accurate for your codebase.
