---
"@abelcondev/spectre": minor
---

Replace the static stack document with a living proposal workflow where Spectre writes all plans (stack, features, architecture) to a single proposal file for human review, then archives approved decisions to a historical record. Add automatic dependency compatibility verification before proposing any dependencies.
