---
"@abelcondev/spectre": minor
---

Fix SDD task flow: add a task creation step after approving proposals, enforce the proposal lifecycle (clear after approval), prefer sdd/tasks/ over TASKS.md when SDD is active, and include a Gherkin task template in the scaffold. Consolidate the agent system prompt to reduce duplication.
