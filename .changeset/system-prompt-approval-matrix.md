---
"@abelcondev/spectre": patch
---

Rewrite the default system prompt to resolve conflicting autonomy instructions, add an explicit approval matrix, and align Context7 config examples with `config.toml`.
