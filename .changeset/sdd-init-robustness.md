---
"@moonshot-ai/kimi-code": patch
---

Make `SddInit` self-bootstrap Git, refuse unintended parent repositories, require a global product definition via `product_answers`, and ship a generic `init.sh` for SDD projects; make `SddWorktree` pause and instruct the human to switch into the new feature worktree.
